# 🎮 战力比拼 - 项目指南

## 📋 目录

1. [项目概述](#项目概述)
2. [技术选型](#技术选型)
3. [项目结构](#项目结构)
4. [AI平台接入指南](#ai平台接入指南)
5. [快速开始](#快速开始)
6. [开发指南](#开发指南)
7. [部署指南](#部署指南)

---

## 项目概述

"战力比拼"是一款基于AI判断的创意对战游戏。玩家需要找出能够"战胜"当前物品的新物品，由AI进行胜负判定，形成一条越来越长的胜利链。

### 核心功能
- 🎯 物品对战判定
- 📊 积分系统
- 📜 历史链条记录
- 🔄 多种游戏模式（规划中）

---

## 技术选型

### 前端技术栈

| 技术 | 版本 | 用途 | 选择理由 |
|-----|------|-----|---------|
| **Vue 3** | ^3.4 | 核心框架 | 轻量、响应式、易上手、中文社区活跃 |
| **TypeScript** | ^5.0 | 类型系统 | 类型安全、代码提示、减少bug |
| **Vite** | ^5.0 | 构建工具 | 极速热更新、开箱即用 |
| **Pinia** | ^2.1 | 状态管理 | Vue3官方推荐、简洁API |
| **TailwindCSS** | ^3.4 | 样式方案 | 原子化CSS、快速开发 |
| **Vue Router** | ^4.2 | 路由管理 | SPA路由、代码分割 |

### 后端技术栈

| 技术 | 版本 | 用途 | 选择理由 |
|-----|------|-----|---------|
| **Node.js** | ^20 | 运行环境 | JavaScript全栈、生态丰富 |
| **Express** | ^4.18 | Web框架 | 轻量、灵活、中间件丰富 |
| **TypeScript** | ^5.0 | 类型系统 | 前后端统一语言 |

> 💡 **为什么选择这套技术栈？**
> - **前后端统一语言**：降低学习成本，提高开发效率
> - **轻量级架构**：适合小型游戏项目，易于部署
> - **社区活跃**：遇到问题容易找到解决方案

### 替代方案

如果您有其他偏好，以下是可替代的技术选项：

| 场景 | 替代方案 |
|-----|---------|
| 前端框架 | React + Zustand、Svelte |
| 后端框架 | Fastify、Koa、Hono |
| 纯前端方案 | 无需后端，直接前端调用AI API（适合个人项目） |

---

## 项目结构

```
战力比拼/
├── frontend/                 # 前端项目
│   ├── src/
│   │   ├── assets/          # 静态资源
│   │   ├── components/      # 通用组件
│   │   │   ├── ItemCard.vue      # 物品卡片
│   │   │   ├── ScoreBoard.vue    # 计分板
│   │   │   └── ChainHistory.vue  # 历史链条
│   │   ├── views/           # 页面视图
│   │   │   ├── HomeView.vue      # 主页
│   │   │   ├── GameView.vue      # 游戏页
│   │   │   └── ResultView.vue    # 结果页
│   │   ├── stores/          # Pinia状态
│   │   │   ├── game.ts           # 游戏状态
│   │   │   └── settings.ts       # 设置状态
│   │   ├── services/        # API服务
│   │   │   └── ai.ts             # AI接口封装
│   │   ├── types/           # 类型定义
│   │   │   └── index.ts
│   │   ├── App.vue
│   │   └── main.ts
│   ├── index.html
│   ├── vite.config.ts
│   ├── tailwind.config.js
│   └── package.json
│
├── backend/                  # 后端项目（可选）
│   ├── src/
│   │   ├── routes/          # 路由
│   │   │   └── game.ts
│   │   ├── services/        # 服务
│   │   │   └── aiService.ts      # AI服务封装
│   │   ├── middleware/      # 中间件
│   │   │   └── rateLimit.ts      # 限流
│   │   ├── types/           # 类型定义
│   │   │   └── index.ts
│   │   └── index.ts         # 入口文件
│   ├── tsconfig.json
│   └── package.json
│
├── shared/                   # 共享代码
│   └── types/               # 共享类型定义
│       └── index.ts
│
├── 项目指南.md               # 本文档
└── README.md
```

---

## AI平台接入指南

### 🆓 默认免费方案：SiliconFlow

[SiliconFlow](https://siliconflow.cn/) 是国内的AI平台，提供免费额度，支持多种开源模型。

#### 优势
- ✅ 免费额度充足（每月数百万token）
- ✅ 无需科学上网
- ✅ 兼容OpenAI API格式
- ✅ 支持多种开源模型（Qwen、GLM等）

#### 注册获取API Key

1. 访问 [https://cloud.siliconflow.cn/](https://cloud.siliconflow.cn/)
2. 注册账号并登录
3. 进入「API密钥」页面
4. 点击「创建密钥」获取API Key

#### 推荐模型

| 模型 | 说明 | 适用场景 |
|-----|------|---------|
| `Qwen/Qwen2.5-7B-Instruct` | 阿里通义千问 | 免费、响应快 |
| `THUDM/glm-4-9b-chat` | 智谱GLM | 免费、中文优化 |
| `deepseek-ai/DeepSeek-V2.5` | 深度求索 | 推理能力强 |

### 🔧 API接入代码示例

#### 方案A：纯前端方案（简单）

```typescript
// frontend/src/services/ai.ts

interface AIConfig {
  apiKey: string;
  baseUrl: string;
  model: string;
}

// 默认配置（SiliconFlow）
const DEFAULT_CONFIG: AIConfig = {
  apiKey: import.meta.env.VITE_AI_API_KEY || '',
  baseUrl: 'https://api.siliconflow.cn/v1',
  model: 'Qwen/Qwen2.5-7B-Instruct'
};

// 自定义配置（从localStorage读取）
function getConfig(): AIConfig {
  const customConfig = localStorage.getItem('ai_config');
  if (customConfig) {
    return { ...DEFAULT_CONFIG, ...JSON.parse(customConfig) };
  }
  return DEFAULT_CONFIG;
}

// 判断物品胜负
export async function judgeItems(
  challenger: string,  // 挑战者
  defender: string     // 被挑战者
): Promise<JudgeResult> {
  const config = getConfig();
  
  const systemPrompt = `你是一个战力比拼游戏的裁判。你需要判断【挑战者】是否能战胜【被挑战者】。

判断规则：
1. 只有明确的"战胜"关系才算成功
2. 平局或无法判定都视为失败
3. 需要有合理的逻辑解释（物理克制、功能克制、概念克制等）
4. 基于常识和合理推断

你必须严格按照以下JSON格式回复，不要包含任何其他内容：
{"result": "win或lose", "reason": "简短理由（15字以内）"}`;

  const userPrompt = `挑战者：${challenger}
被挑战者：${defender}

请判断挑战者是否能战胜被挑战者。`;

  const response = await fetch(`${config.baseUrl}/chat/completions`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${config.apiKey}`
    },
    body: JSON.stringify({
      model: config.model,
      messages: [
        { role: 'system', content: systemPrompt },
        { role: 'user', content: userPrompt }
      ],
      temperature: 0.3,  // 降低随机性，保证判断一致性
      max_tokens: 100
    })
  });

  if (!response.ok) {
    throw new Error(`AI请求失败: ${response.status}`);
  }

  const data = await response.json();
  const content = data.choices[0].message.content;
  
  try {
    return JSON.parse(content);
  } catch {
    // 如果解析失败，尝试提取关键信息
    const isWin = content.includes('win') || content.includes('战胜') || content.includes('胜利');
    return {
      result: isWin ? 'win' : 'lose',
      reason: content.slice(0, 20)
    };
  }
}

// 类型定义
export interface JudgeResult {
  result: 'win' | 'lose';
  reason: string;
}
```

#### 方案B：通过后端代理（安全）

```typescript
// backend/src/services/aiService.ts

import fetch from 'node-fetch';

interface AIConfig {
  apiKey: string;
  baseUrl: string;
  model: string;
}

class AIService {
  private config: AIConfig;

  constructor() {
    this.config = {
      apiKey: process.env.AI_API_KEY || '',
      baseUrl: process.env.AI_BASE_URL || 'https://api.siliconflow.cn/v1',
      model: process.env.AI_MODEL || 'Qwen/Qwen2.5-7B-Instruct'
    };
  }

  async judgeItems(challenger: string, defender: string) {
    const systemPrompt = `你是一个战力比拼游戏的裁判...`; // 同上
    const userPrompt = `挑战者：${challenger}\n被挑战者：${defender}`;

    const response = await fetch(`${this.config.baseUrl}/chat/completions`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${this.config.apiKey}`
      },
      body: JSON.stringify({
        model: this.config.model,
        messages: [
          { role: 'system', content: systemPrompt },
          { role: 'user', content: userPrompt }
        ],
        temperature: 0.3,
        max_tokens: 100
      })
    });

    const data = await response.json();
    return JSON.parse(data.choices[0].message.content);
  }
}

export const aiService = new AIService();
```

```typescript
// backend/src/routes/game.ts

import { Router } from 'express';
import { aiService } from '../services/aiService';

const router = Router();

router.post('/judge', async (req, res) => {
  try {
    const { challenger, defender } = req.body;
    
    if (!challenger || !defender) {
      return res.status(400).json({ error: '缺少参数' });
    }

    const result = await aiService.judgeItems(challenger, defender);
    res.json(result);
  } catch (error) {
    console.error('AI判断失败:', error);
    res.status(500).json({ error: 'AI服务暂时不可用' });
  }
});

export default router;
```

### 🔄 支持多平台切换

```typescript
// frontend/src/services/ai.ts

// 预设的AI平台配置
export const AI_PRESETS = {
  siliconflow: {
    name: 'SiliconFlow (免费)',
    baseUrl: 'https://api.siliconflow.cn/v1',
    models: [
      { id: 'Qwen/Qwen2.5-7B-Instruct', name: 'Qwen2.5-7B (推荐)' },
      { id: 'THUDM/glm-4-9b-chat', name: 'GLM-4-9B' },
      { id: 'deepseek-ai/DeepSeek-V2.5', name: 'DeepSeek-V2.5' }
    ]
  },
  openai: {
    name: 'OpenAI',
    baseUrl: 'https://api.openai.com/v1',
    models: [
      { id: 'gpt-4o-mini', name: 'GPT-4o Mini' },
      { id: 'gpt-4o', name: 'GPT-4o' }
    ]
  },
  deepseek: {
    name: 'DeepSeek',
    baseUrl: 'https://api.deepseek.com/v1',
    models: [
      { id: 'deepseek-chat', name: 'DeepSeek Chat' }
    ]
  },
  custom: {
    name: '自定义',
    baseUrl: '',
    models: []
  }
};

// 设置AI配置
export function setAIConfig(config: Partial<AIConfig>) {
  localStorage.setItem('ai_config', JSON.stringify(config));
}

// 获取当前配置
export function getAIConfig(): AIConfig {
  const saved = localStorage.getItem('ai_config');
  if (saved) {
    return JSON.parse(saved);
  }
  return {
    apiKey: '',
    baseUrl: AI_PRESETS.siliconflow.baseUrl,
    model: AI_PRESETS.siliconflow.models[0].id
  };
}
```

### ⚙️ 环境变量配置

#### 前端 (.env)

```env
# AI配置（可选，也可以在界面中设置）
VITE_AI_API_KEY=your_api_key_here
VITE_AI_BASE_URL=https://api.siliconflow.cn/v1
VITE_AI_MODEL=Qwen/Qwen2.5-7B-Instruct
```

#### 后端 (.env)

```env
# 服务器配置
PORT=3000
NODE_ENV=development

# AI配置
AI_API_KEY=your_api_key_here
AI_BASE_URL=https://api.siliconflow.cn/v1
AI_MODEL=Qwen/Qwen2.5-7B-Instruct

# 限流配置
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=30
```

---

## 快速开始

### 环境要求

- Node.js >= 18
- pnpm >= 8 (推荐) 或 npm >= 9

### 方式一：纯前端运行（最简单）

```bash
# 1. 进入前端目录
cd frontend

# 2. 安装依赖
pnpm install

# 3. 配置环境变量
cp .env.example .env
# 编辑 .env 填入你的 API Key

# 4. 启动开发服务器
pnpm dev

# 5. 打开浏览器访问 http://localhost:5173
```

### 方式二：前后端分离运行

```bash
# 终端1：启动后端
cd backend
pnpm install
cp .env.example .env
# 编辑 .env 填入配置
pnpm dev

# 终端2：启动前端
cd frontend
pnpm install
pnpm dev
```

---

## 开发指南

### 核心游戏逻辑

```typescript
// frontend/src/stores/game.ts

import { defineStore } from 'pinia';
import { judgeItems, type JudgeResult } from '@/services/ai';

interface ChainItem {
  item: string;
  defeatedBy: string;
  reason: string;
}

interface GameState {
  currentItem: string;
  score: number;
  chain: ChainItem[];
  status: 'idle' | 'playing' | 'judging' | 'ended';
  lastResult: JudgeResult | null;
}

export const useGameStore = defineStore('game', {
  state: (): GameState => ({
    currentItem: '石头',
    score: 0,
    chain: [],
    status: 'idle',
    lastResult: null
  }),

  actions: {
    // 开始新游戏
    startGame() {
      this.currentItem = '石头';
      this.score = 0;
      this.chain = [];
      this.status = 'playing';
      this.lastResult = null;
    },

    // 发起挑战
    async challenge(item: string) {
      if (this.status !== 'playing') return;
      
      this.status = 'judging';
      
      try {
        const result = await judgeItems(item, this.currentItem);
        this.lastResult = result;

        if (result.result === 'win') {
          // 挑战成功
          this.chain.push({
            item: this.currentItem,
            defeatedBy: item,
            reason: result.reason
          });
          this.currentItem = item;
          this.score += 1;
          this.status = 'playing';
        } else {
          // 挑战失败，游戏结束
          this.status = 'ended';
        }
      } catch (error) {
        console.error('AI判断出错:', error);
        this.status = 'playing'; // 出错时回到playing状态
        throw error;
      }
    },

    // 重新开始
    restart() {
      this.startGame();
    }
  }
});
```

### 添加新功能清单

- [ ] 本地最高分存储
- [ ] 音效系统
- [ ] 动画效果
- [ ] 分享功能
- [ ] 排行榜

---

## 部署指南

### Vercel 部署（推荐）

1. 将代码推送到 GitHub
2. 在 Vercel 导入项目
3. 设置环境变量
4. 部署

### Docker 部署

```dockerfile
# frontend/Dockerfile
FROM node:20-alpine as builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
```

---

## 常见问题

### Q: API调用失败怎么办？
A: 检查API Key是否正确，网络是否通畅。如果使用国外服务（如OpenAI），需要配置代理。

### Q: 如何降低API费用？
A: 使用SiliconFlow等提供免费额度的平台，或选择更小的模型。

### Q: 可以离线运行吗？
A: 目前需要在线调用AI API。离线版本需要本地部署LLM，技术复杂度较高。

---

## 参考链接

- [Vue 3 文档](https://vuejs.org/)
- [Vite 文档](https://vitejs.dev/)
- [SiliconFlow API文档](https://docs.siliconflow.cn/)
- [Pinia 文档](https://pinia.vuejs.org/)
- [TailwindCSS 文档](https://tailwindcss.com/)

---

*最后更新: 2025年1月*